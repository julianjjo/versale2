import type { Metadata } from "next";

export const metadata: Metadata = { title: "Crear cuenta — Versale" };

export default function SignupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
